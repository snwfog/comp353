{% block title %}

    <title>

        {# title: the actual title of the website, i.e. Index, Member, Login #}
        {# specifier: could append a specifier to the title when relevant #}
        {#            but usually should be "COMP 353" or "AuctionMax" #}
        {{ title }} - {{ specifier }}

    </title>

{% endblock title %}